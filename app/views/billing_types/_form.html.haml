= simple_form_for @billing_type, wrapper: :bootstrap do |f|
  = f.error_notification
  .form-group
    = f.input :name, label: "Name", input_html: { class: 'form-control' }
  .form-actions
    = f.button :submit, class: 'btn btn-primary'
