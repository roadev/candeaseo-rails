= form_with(model: state, local: true) do |f|
  %div.row
    %div.col-md-6.form-group
      = f.label :name, "Nombre", class: "form-label"
      = f.text_field :name, class: "form-control", placeholder: "Ingrese el nombre del estado"

    %div.col-md-6.form-group
      = f.label :country_id, "País", class: "form-label"
      = f.collection_select :country_id, Country.all, :id, :name, { prompt: "Seleccione un país" }, class: "form-control"

  %div.row.mt-3
    %div.col-md-6.form-group
      = f.label :iso_code, "Código ISO", class: "form-label"
      = f.text_field :iso_code, class: "form-control", placeholder: "Ingrese el código ISO del estado"

    %div.col-md-6.form-check
      = f.check_box :active, class: "form-check-input"
      = f.label :active, "Activo", class: "form-check-label"

  %div.mt-3
    = f.submit "Guardar", class: "btn btn-primary"
    = link_to "Cancelar", states_path, class: "btn btn-secondary ml-2"
