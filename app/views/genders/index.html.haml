%h1.text-center.mt-4 Listado de Géneros

%div.table-responsive
  %table.table.table-bordered.table-striped
    %thead.thead-dark
      %tr
        %th Nombre
        %th Nombre Corto
        %th Orden
        %th Activo
        %th Acciones

    %tbody
      - @genders.each do |gender|
        %tr
          %td= gender.name
          %td= gender.short_name
          %td= gender.display_order
          %td= gender.active ? "Sí" : "No"
          %td
            = link_to 'Ver', gender, class: 'btn btn-primary btn-sm'
            = link_to 'Editar', edit_gender_path(gender), class: 'btn btn-warning btn-sm mx-2'
            = link_to "Eliminar", gender_path(gender), method: :delete, data: { turbo_method: "delete", confirm: "¿Estás seguro?" }, class: "btn btn-danger btn-sm"

%div.mt-4
  = link_to 'Nuevo Género', new_gender_path, class: 'btn btn-success'
