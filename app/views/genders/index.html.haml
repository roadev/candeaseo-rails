%h1.text-center.mt-4 Listado de Géneros

%div.table-responsive
  %table.table.table-bordered.table-striped
    %thead.thead-dark
      %tr
        %th Nombre
        %th Nombre Corto
        %th Orden
        %th Activo
        %th Acciones

    %tbody
      - @genders.each do |gender|
        %tr
          %td= gender.name
          %td= gender.short_name
          %td= gender.display_order
          %td= gender.active ? "Sí" : "No"
          %td
            = link_to 'Ver', gender, class: 'btn btn-sm btn-primary'
            = link_to 'Editar', edit_gender_path(gender), class: 'btn btn-sm btn-warning mx-2'
            = link_to 'Eliminar', gender, method: :delete, data: { confirm: '¿Estás seguro?' }, class: 'btn btn-sm btn-danger'

%div.mt-4
  = link_to 'Nuevo Género', new_gender_path, class: 'btn btn-success'
