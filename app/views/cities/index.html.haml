%h1.text-center.mt-4 Listado de Ciudades

%div.table-responsive
  %table.table.table-bordered.table-striped
    %thead.thead-dark
      %tr
        %th Nombre
        %th Estado
        %th País
        %th Activo
        %th Acciones

    %tbody
      - @cities.each do |city|
        %tr
          %td= city.name
          %td= city.state&.name
          %td= city.country&.name
          %td= city.active ? "Sí" : "No"
          %td
            = link_to 'Ver', city, class: 'btn btn-primary btn-sm'
            = link_to 'Editar', edit_city_path(city), class: 'btn btn-warning btn-sm mx-2'
            = link_to "Eliminar", city_path(city), method: :delete, data: { turbo_method: "delete", confirm: "¿Estás seguro?" }, class: "btn btn-danger btn-sm"

%div.mt-4
  = link_to 'Nueva Ciudad', new_city_path, class: 'btn btn-success'
