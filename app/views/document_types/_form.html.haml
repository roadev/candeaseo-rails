= form_with(model: document_type, local: true) do |f|
  %div.row
    %div.col-md-6.form-group
      = f.label :name, "Nombre", class: "form-label"
      = f.text_field :name, class: "form-control", placeholder: "Ingrese el nombre"

    %div.col-md-6.form-group
      = f.label :short_name, "Nombre Corto", class: "form-label"
      = f.text_field :short_name, class: "form-control", placeholder: "Ingrese el nombre corto"

  %div.row
    %div.col-md-6.form-group
      = f.label :display_order, "Orden de Visualización", class: "form-label"
      = f.number_field :display_order, class: "form-control", placeholder: "Ingrese el orden"

    %div.col-md-6.form-check
      = f.check_box :active, class: "form-check-input"
      = f.label :active, "Activo", class: "form-check-label"

  %div.mt-3
    = f.submit "Guardar", class: "btn btn-primary"
    = link_to "Cancelar", document_types_path, class: "btn btn-secondary ml-2"
